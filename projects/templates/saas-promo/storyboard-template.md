# {{PRODUCT_NAME}} — Storyboard (generated)

> Generated by `build.py`. Source of truth is `projects/<name>/config.json` plus
> the template files in `projects/templates/saas-promo/`. Do not edit by hand.

- **Format:** 1920×1080 @ 30fps · duration auto-fits the scene timings + 1s tail.
- **Composition:** existing Remotion `Explainer` (OpenMontage core — unmodified).
- **Palette:** background dark, primary `{{PRIMARY_COLOR}}` (workflow), success
  `{{SUCCESS_COLOR}}` (trust + close). Animated gradient/orb background shows
  through transparent scene cards.
- **Motion:** calm — fade / slide / scale / subtle drift only.

## Scenes

{{SCENES_TABLE}}

## CTA overlay

A bottom-left lower-third appears partway into the final scene showing the trust
badge and **{{CTA_URL}}** in the success color.

## Reuse note

Built entirely from existing components (`StatCard`, `SectionTitle`) + the
animated background. Brand identity is injected purely through config-driven
`themeConfig` — no OpenMontage core files are modified.
